PORT=3100
# Chave secreta — gere algo longo e aleatório, ex: openssl rand -hex 32
API_KEY=troque-por-uma-chave-secreta-longa

# Variáveis abaixo são definidas automaticamente pelo Dockerfile — não altere
# PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
# PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium
